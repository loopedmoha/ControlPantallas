using MySqlConnector;
using System.Data.SqlClient;

namespace SuperfaldonWinUI.Data
{
    public static class DbConnectionFactory
    {
        /*public static MySqlConnection CreateConnection()
        {
            string ip = IniConfiguration.Get("ip");
            string database = IniConfiguration.Get("database");
            string user = IniConfiguration.Get("user");
            string password = IniConfiguration.Get("password");

            string connectionString =
                $"Server={ip};Database={database};User={user};Password={password};Port=3306;SslMode=None;";

            return new MySqlConnection(connectionString);
        }
    }*/
    }
}
